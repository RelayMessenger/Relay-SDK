{
  "name": "relaymessenger-dev",
  "private": true,
  "workspaces": [
    "packages/relaymessenger",
    "packages/sdk",
    "integrations/claude-code",
    "integrations/openclaw",
    "integrations/vercel-ai",
    "examples/plugins/hermes",
    "examples/raw-webhook-agent",
    "examples/showcase-agent"
  ],
  "scripts": {
    "check": "npm run check --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "validate": "npm run build --workspace @relaymessenger/sdk && npm run check && npm run build && npm test",
    "manifests:check": "node examples/harnesses/validate-manifests.mjs",
    "sdk:consumer-smoke": "node examples/harnesses/packed-consumer.mjs",
    "e2e:production": "node examples/harnesses/e2e-production.mjs",
    "validate:claude-plugin": "npm exec --workspace @relaymessenger/cli -- claude plugin validate ../../integrations/claude-code --strict && npm exec --workspace @relaymessenger/cli -- claude plugin validate ../../ --strict",
    "test:release": "node --test scripts/*.test.mjs",
    "runtime:smoke": "node scripts/acp-runtime-smoke.mjs",
    "gateway:harness": "npm run gateway:harness --workspace @relaymessenger/openclaw-plugin",
    "pack:check": "node scripts/relaymessenger-release.mjs pack-json-smoke && node scripts/pack-smoke.mjs && npm run pack:smoke --workspace relay-claude-channel && npm run pack:smoke --workspace @relaymessenger/openclaw-plugin"
  },
  "devDependencies": {
    "@hono/node-server": "^2.1.0",
    "@types/node": "^26.2.0",
    "tsx": "^4.23.11",
    "typescript": "^7.0.2",
    "undici": "^8.10.0",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "openclaw": {
      "undici": "^8.10.0"
    },
    "undici": "^8.10.0"
  }
}