interface:
  display_name: "Relay"
  short_description: "Set up Relay agents and build messaging integrations"
  brand_color: "#111111"
  default_prompt: "Use $relay to set up or implement this Relay agent integration from the locked contract."

dependencies:
  tools:
    - type: "mcp"
      value: "relayDocs"
      description: "Relay public documentation search and retrieval"
      transport: "streamable_http"
      url: "https://docs.staging.relayapp.im/mcp"
